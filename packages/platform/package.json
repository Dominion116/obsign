{
  "name": "@obsign/platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Obsign platform — framework-agnostic domain + infra (Mongo cache/queue, SIWE, evidence store, chain indexer, credential service). All impure I/O lives here, never in @obsign/core (INV-1). Mongo is cache only (INV-4).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@obsign/core": "0.1.0",
    "@obsign/sdk": "0.1.0",
    "ethers": "^6.13.0",
    "jose": "^5.9.0",
    "mongodb": "^6.9.0",
    "siwe": "^2.3.2",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "mongodb-memory-server": "^10.1.2"
  }
}
