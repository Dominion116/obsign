{
  "name": "@obsign/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Obsign SDK — viem-based live chain reader. Fetches a pinned snapshot (INV-6) and produces a synchronous core ChainReader. viem lives here, never in @obsign/core (INV-1).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@obsign/core": "0.1.0",
    "viem": "^2.21.0"
  }
}
